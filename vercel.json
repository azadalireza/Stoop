{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm install && next build"
      }
    }
  ],
  "installCommand": "npm install"
}
